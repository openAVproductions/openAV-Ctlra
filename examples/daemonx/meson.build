example_src = files('daemonx.c')
dependencies += [cairo_dep, midi_dep, m_dep]
install = true

# TODO: review and if required, always enable
#if(get_option('usbfix') == true)
#    c_args = ['-DTEMP_USB_FIX=1']
#endif
